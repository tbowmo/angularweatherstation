/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TabStreamsComponent } from './tab-streams.component';

describe('StreamsComponent', () => {
  let component: TabStreamsComponent;
  let fixture: ComponentFixture<TabStreamsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TabStreamsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TabStreamsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
