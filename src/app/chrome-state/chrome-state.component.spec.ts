/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ChromeStateComponent } from './chrome-state.component';

describe('ChromeStateComponent', () => {
  let component: ChromeStateComponent;
  let fixture: ComponentFixture<ChromeStateComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ChromeStateComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ChromeStateComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
