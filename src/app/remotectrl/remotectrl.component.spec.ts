/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RemotectrlComponent } from './remotectrl.component';

describe('RemotectrlComponent', () => {
  let component: RemotectrlComponent;
  let fixture: ComponentFixture<RemotectrlComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RemotectrlComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RemotectrlComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
